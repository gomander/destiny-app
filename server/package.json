{
  "name": "darci-server",
  "version": "1.0.0",
  "description": "Express backend for DARCI.gg",
  "author": "Gunnar Omander <gunnar@gomander.dev>",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon src/app.ts",
    "build": "tsc && npm run copy-package && npm run copy-docker && npm run copy-env",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "npm run build",
    "deploy": "deploy-prod.bat",
    "copy-package": "copy package*.json dist || cp package*.json dist",
    "copy-docker": "copy Dockerfile dist || cp Dockerfile dist",
    "copy-env": "copy .env* dist || cp .env* dist"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/dotenv-flow": "^3.2.0",
    "@types/express": "^4.17.17",
    "@types/morgan": "^1.9.4",
    "@types/node": "^20.3.3",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@google-cloud/firestore": "^6.6.1",
    "cors": "^2.8.5",
    "dotenv-flow": "^3.2.0",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "morgan": "^1.10.0"
  }
}
